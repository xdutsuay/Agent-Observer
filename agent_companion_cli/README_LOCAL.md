# Agent Companion CLI (Local)

This CLI runs locally on your machine and observes AI agent activity.

## Install
pip install -e .

## Run
agent-companion start

## Output
All data is written under the configured output_root.

UI reads generated files in read-only mode.
